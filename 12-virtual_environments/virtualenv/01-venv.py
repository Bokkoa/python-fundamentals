from cowpy import cow

cow_instance = cow.Cowacter()

print(cow_instance.milk("Python is great!"))

# we can have the libraries listed in requirements.txt
# using 
# pip freeze > requirements.txt
# that makes a libraries snapshot 